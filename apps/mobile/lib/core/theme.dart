import 'package:flutter/material.dart';

ThemeData buildTheme() => ThemeData(colorScheme: ColorScheme.fromSeed(seedColor: const Color(0xFF40D9A5), brightness: Brightness.dark), scaffoldBackgroundColor: const Color(0xFF07110F), useMaterial3: true);
