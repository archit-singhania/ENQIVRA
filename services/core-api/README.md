# Core API

Spring Boot owns transactional business data and future identity, organizations, assets, and diagnostic cases.

```bash
mvn spring-boot:run
mvn test
mvn spotless:check
```

Set `DB_URL`, `DB_USER`, and `DB_PASSWORD`, or use Docker Compose from the repository root.
